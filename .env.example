# Docsiphon currently has no required environment variables.
#
# Keep runtime configuration in CLI flags or a TOML profile file unless a new
# feature explicitly introduces an environment contract.
#
# Example:
# DOCSIPHON_PROFILE=./config.toml
