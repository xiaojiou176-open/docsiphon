# Docsiphon Public Skills

This directory holds host-native public skill packets for Docsiphon.

The flagship public story is still:

- GitHub repo front door
- `uvx` quickstart
- release assets / example profiles

These packets are **secondary lanes** for host ecosystems that want a
self-contained folder describing how to run Docsiphon safely and inspect the
resulting export artifacts.

Current packet inventory:

- [`docsiphon-doc-corpus-operator/`](./docsiphon-doc-corpus-operator/README.md)

Current registry truth:

- `ClawHub`: `not submitted yet`
- `OpenHands/extensions`: `not submitted yet`

That wording is intentional.
This folder is packet-ready, not listing-ready.
